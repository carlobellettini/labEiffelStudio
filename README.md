#Punto di partenza per laboratorio Eiffel

rispetto a installazione eiffelstudio aggiunge:

* terminale  `rxvt`
* `git`
* GUI per git `gitg`

* alias `git hist` 
* external command (menu di EiffelStudio  Tools) per gitg e rxvt